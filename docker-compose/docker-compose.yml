version: '2'

networks:

  front-trier: {}
  back-trier: {}

services:

  mongodb:
    image: mongo:3.4
    container_name: mongodb
    restart: always
    expose:
        - "27017"
    networks:
      - back-trier
    volumes:
      - database:/data/db
    command: mongod --replSet "rs0"

  solr:
    image: slidewiki/solr:latest
    container_name: solr
    restart: always
    environment:
      - VIRTUAL_PORT=8983
    expose:
      - "8983"
    networks:
      - front-trier
      - back-trier
    depends_on:
      - mongodb

  webserver:
    image: slidewiki/webproxy
    restart: always
    ports:
      - "80:80"
      - "443:443"
    networks:
      - front-trier
    volumes:
      - /var/run/docker.sock:/tmp/docker.sock:ro
      - ./htpasswd:/etc/nginx/htpasswd

  platform:
    image: slidewiki/platform:latest
    restart: always
    expose:
      - "80"
    environment:
      - PORT=80
    networks:
      - front-trier
    depends_on:
      - webserver
      - deckservice
      - activitiesservice
      - discussionservice
      - notificationservice
      - importservice
      - userservice
      - pdfservice
      - fileservice
      - searchservice
      - tagservice

  deckservice:
    image: slidewiki/deckservice:latest
    restart: always
    expose:
      - "80"
    networks:
      - front-trier
      - back-trier
    environment:
      - APPLICATION_PORT=80
      - DATABASE_PORT=27017
      - DATABASE_URL=mongodb
    depends_on:
      - fileservice
      - mongodb

  unoconvservice:
    image: zrrrzzt/docker-unoconv-webservice
    restart: always
    environment:
      - VIRTUAL_PORT=3000
      - PAYLOAD_MAX_SIZE=314572800
    expose:
      - "3000"
    networks:
      - front-trier
      - back-trier

  activitiesservice:
    image: slidewiki/activitiesservice:latest
    restart: always
    expose:
      - "80"
    networks:
      - front-trier
      - back-trier
    environment:
      - APPLICATION_PORT=80
      - DATABASE_PORT=27017
      - DATABASE_URL=mongodb
    depends_on:
      - deckservice
      - userservice
      - mongodb

  discussionservice:
    image: slidewiki/discussionservice:latest
    restart: always
    expose:
      - "80"
    networks:
      - front-trier
      - back-trier
    environment:
      - APPLICATION_PORT=80
      - DATABASE_PORT=27017
      - DATABASE_URL=mongodb
    depends_on:
      - activitiesservice
      - notificationservice
      - userservice
      - mongodb

  notificationservice:
    image: slidewiki/notificationservice:latest
    restart: always
    expose:
      - "80"
    networks:
      - front-trier
      - back-trier
    environment:
      - APPLICATION_PORT=80
      - DATABASE_PORT=27017
      - DATABASE_URL=mongodb
    depends_on:
      - userservice
      - mongodb

  importservice:
    image: slidewiki/importservice:latest
    restart: always
    expose:
      - "80"
    networks:
      - front-trier
      - back-trier
    environment:
      - APPLICATION_PORT=80
      - DATABASE_PORT=27017
      - DATABASE_URL=mongodb
    depends_on:
      - deckservice
      - fileservice
      - mongodb

  userservice:
    image: slidewiki/userservice:latest
    restart: always
    expose:
      - "80"
    networks:
      - front-trier
      - back-trier
    environment:
      - APPLICATION_PORT=80
      - DATABASE_PORT=27017
      - DATABASE_URL=mongodb
    depends_on:
      - mongodb

  pdfservice:
    image: slidewiki/pdfservice:latest
    restart: always
    expose:
      - "80"
    networks:
      - front-trier
    environment:
      - APPLICATION_PORT=80
    depends_on:
      - deckservice

  fileservice:
    image: slidewiki/fileservice:latest
    restart: always
    expose:
      - "80"
    networks:
      - front-trier
      - back-trier
    environment:
      - APPLICATION_PORT=80
      - APPLICATION_PATH=/data/files
      - DATABASE_PORT=27017
      - DATABASE_URL=mongodb
    depends_on:
      - mongodb

  searchservice:
    image: slidewiki/searchservice:latest
    restart: always
    expose:
      - "80"
    networks:
      - front-trier
      - back-trier
    environment:
      - APPLICATION_PORT=80
      - DATABASE_PORT=27017
      - DATABASE_URL=mongodb
      - SOLR_CORE=swikcore
      - SOLR_HOST=solr
    depends_on:
      - userservice
      - solr
      - mongodb

  #xapi:
  #  image: slidewiki/xapi-service:latest
  #  restart: always
  #  expose:
  #    - "80"
  #  networks:
  #    - front-trier
  #  environment:
  #    - APPLICATION_PORT=80

  tagservice:
    image: slidewiki/tagservice:latest
    restart: always
    expose:
      - "80"
    networks:
      - front-trier
      - back-trier
    environment:
      - APPLICATION_PORT=80
      - DATABASE_PORT=27017
      - DATABASE_URL=mongodb
    depends_on:
      - mongodb

  nlpstore:
    image: slidewiki/nlpstore:latest
    restart: always
    expose:
      - "80"
    networks:
      - front-trier
      - back-trier
    environment:
      - APPLICATION_PORT=80
      - DATABASE_PORT=27017
      - DATABASE_URL=mongodb
    depends_on:
      - mongodb
